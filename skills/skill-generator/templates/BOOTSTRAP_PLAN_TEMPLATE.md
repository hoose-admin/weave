# Bootstrap Plan — {{target}}

**Generated:** {{date}}
**Target repo:** {{repo-root-path}}
**Existing skill count:** {{existing-skill-count}}
**Proposed-new skill count:** {{proposed-count}}
**Validator verdict:** {{pass | fail}}

---

## 1. Signal summary

### Deploy units detected

| Unit | Framework | Evidence |
|---|---|---|
| {{name}} | {{framework}} | {{file:line}} |

### Data layers detected

| Layer | Read | Write | Evidence |
|---|---|---|---|
| {{name}} | ✓/✗ | ✓/✗ | {{file:line}} |

### Cross-cutting concerns detected

| Concern | Evidence |
|---|---|
| {{name}} | {{file:line}} |

### Deploy targets detected

| Target | Evidence |
|---|---|
| {{name}} | {{file:line}} |

---

## 2. Proposed portfolio

Organized by cluster. Each cluster is a candidate orchestrator + members, or a flat family if no orchestrator is justified.

### Cluster: {{cluster-name}}

- **Orchestrator:** {{slug}} (`kind: orchestrator`) — {{rationale}}
- **Members:**
  - `{{slug}}` (`kind: audit`) — {{one-line purpose, traced to signal}}
  - `{{slug}}` (`kind: action`) — {{one-line purpose, traced to signal}}
- **Connects-to edges:**
  - `{{slug-a}}` → `{{slug-b}}` ({{reason}})
- **Evidence-anchored rationale:** {{which signals justify this cluster}}

(repeat per cluster)

---

## 3. Gap findings vs existing portfolio

Candidates that the heuristics would propose but were **skipped** because the existing portfolio already covers them (per `skill-builder.overlap-check`):

| Candidate | Existing skill | Coverage match | Notes |
|---|---|---|---|
| `{{slug}}` | `{{existing-slug}}` | full / partial | {{notes}} |

Candidates that the heuristics would propose AND are **genuinely missing** from the existing portfolio (these become child tickets):

| Candidate | Justifying signal | Cluster |
|---|---|---|
| `{{slug}}` | {{signal}} | {{cluster}} |

---

## 4. Harness extras

| Component | Status | Rationale |
|---|---|---|
| `CLAUDE.md` | exists / missing | — |
| `.tickets/` flow | exists / missing | — |
| `.weave/`-style dashboard | exists / missing / not justified (<10 skills) | — |
| Convention files (`NUMERIC_CONVENTIONS.md`, etc.) | exists / missing / not justified | — |
| `ADRs/` | exists / missing | — |

---

## 5. Validator report

**Validator subagent:** `general-purpose` (fresh context, cold-reader)
**Axes:**

| Axis | Pass | Evidence |
|---|---|---|
| Signal fidelity | ✓/✗ | {{evidence}} |
| Overlap correctness | ✓/✗ | {{evidence}} |
| Cluster sanity | ✓/✗ | {{evidence}} |
| Harness justification | ✓/✗ | {{evidence}} |

**Flagged items:**
- {{item}} — {{reason}}

**Verdict:** {{pass | fail}}

---

## 6. Ordered child-ticket list

If the user approves this plan AND invokes `emit-child-tickets`, the following tickets will be filed (in this order, so dependencies are respected):

1. `{{slug}}` — domain: `{{domain}}`, complexity: `{{int}}`, related: `[TKT-NNN, ...]`
2. `{{slug}}` — …

Each child ticket inherits the AC template from `PORTFOLIO_HEURISTICS.md § Per-skill AC template` unless flagged otherwise above.

---

## 7. Introspection notes

Any findings the introspection subagent surfaced that didn't fit a heuristic, or evidence gaps the user should be aware of.

- {{note}}

---

*Generated by `skill-generator` ({{skill-version}}). Plan is propose-only; no skill files have been written. Run `emit-child-tickets` after review to file child tickets.*
