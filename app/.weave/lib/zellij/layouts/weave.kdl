// weave's zellij layout: one bare pane, NO tab-bar/status-bar plugins — the
// weave dashboard owns all chrome, so the terminal is the entire screen (this
// also leaves the full screen as one scroll region for xterm.js).
layout {
    pane
}
